# This file is kept in sync with the root Dockerfile.
# The root Dockerfile is the canonical version used by docker-compose.
# This copy exists for tools that expect a Dockerfile inside apps/api/.
#
# If you need to change the build, edit the ROOT Dockerfile instead.

# See: ../../Dockerfile
